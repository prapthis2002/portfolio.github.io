---
import { Image, getImage } from 'astro:assets';
import Layout from '../layouts/Layout.astro';
import toteFlat from '../assets/Grojet/tote-flat.webp';
import festiveVishu from '../assets/Grojet/festive-vishu.webp';
import festiveRamNavami from '../assets/Grojet/festive-ramnavami.webp';
import festiveEid from '../assets/Grojet/festive-eid.webp';
import merchantNila from '../assets/Grojet/merchant-nila.webp';
import merchantCanara from '../assets/Grojet/merchant-canara.webp';
import merchantBhatnbhat from '../assets/Grojet/merchant-bhatnbhat.webp';
import tyCard from '../assets/Grojet/ty-card.webp';

const optimizedHeroBg = await getImage({ src: festiveVishu, width: 1920, format: 'webp', quality: 75 });

const checklist = [
  'Celebrate festivals and local culture',
  'Support local merchants and partnerships',
  'Create memorable unboxing & brand moments',
];

const stats = [
  { value: '15+', label: 'Design Assets' },
  { value: '6+', label: 'Campaigns' },
  { value: '100%', label: 'Mangalore Inspired' },
];

const categories = [
  {
    number: '01',
    key: 'festival',
    title: 'Festival Posters',
    description: 'Celebrating festivals with local flavor and festive vibes.',
    layout: 'row3',
    images: [
      { src: festiveRamNavami, alt: 'Happy Ram Navami festive poster' },
      { src: festiveEid, alt: 'Eid Mubarak festive poster' },
      { src: festiveVishu, alt: 'Happy Vishu festive poster' },
    ],
  },
  {
    number: '02',
    key: 'merchant',
    title: 'Merchant Stories',
    description: 'Highlighting local businesses we partner with.',
    layout: 'row3',
    images: [
      { src: merchantCanara, alt: 'Grojet x Canara Farms fresh milk campaign' },
      { src: merchantBhatnbhat, alt: "Grojet x Bhat 'n' Bhat pickle campaign" },
      { src: merchantNila, alt: 'Grojet x Nila coffee & brownie campaign' },
    ],
  },
  {
    number: '03',
    key: 'thankyou',
    title: 'Thank You Card',
    description: 'Turning unboxing into a fun and interactive experience.',
    layout: 'single',
    images: [
      { src: tyCard, alt: 'Grojet thank-you card with Mangalore-themed puzzle' },
    ],
  },
  {
    number: '04',
    key: 'merchandise',
    title: 'Merchandise',
    description: 'Mangalore doodle tote bag design.',
    layout: 'single',
    compact: true,
    images: [
      { src: toteFlat, alt: 'Grojet branded tote bag flat design' },
    ],
  },
];

const filters = [
  { label: 'All', key: 'all' },
  ...categories.map((c) => ({ label: c.title, key: c.key })),
];

const designProcess = [
  { label: 'Research & Inspiration', icon: 'M11 4a7 7 0 1 0 0 14 7 7 0 0 0 0-14zM21 21l-4.3-4.3' },
  { label: 'Concept & Sketching', icon: 'M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z' },
  { label: 'Design & Iteration', icon: 'M20.6 13.4 12.5 21.5a2 2 0 0 1-2.8 0l-6.2-6.2a2 2 0 0 1 0-2.8L11.6 4.4a2 2 0 0 1 1.4-.6H19a2 2 0 0 1 2 2v6.2a2 2 0 0 1-.4 1.4zM16.5 8.5h.01' },
  { label: 'Final Delivery', icon: 'M19 21H5a2 2 0 0 1-2-2V8l5-5h8l5 5v11a2 2 0 0 1-2 2zM8 3v5H3M8 13h8' },
];

const impact = [
  { label: 'Stronger local brand presence', icon: 'M12 2 4 5v6c0 5.5 3.8 8.7 8 11 4.2-2.3 8-5.5 8-11V5l-8-3z' },
  { label: 'Enhanced customer engagement', icon: 'M7 14a3 3 0 1 0 0-6 3 3 0 0 0 0 6zM17 14a3 3 0 1 0 0-6 3 3 0 0 0 0 6zM7 14c-2.2 0-4 1.5-4 3.5V19h8v-1.5c0-2-1.8-3.5-4-3.5zM17 14c-.8 0-1.6.15-2.3.45.8.85 1.3 1.95 1.3 3.05V19h6v-1.5c0-2-1.8-3.5-4-3.5z' },
  { label: 'Built emotional connection', icon: 'M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z' },
];
---

<Layout>
  <div class="project-page">
    <a href="/" class="back-button" data-cursor-hover>
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M19 12H5M5 12L12 19M5 12L12 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      BACK
    </a>

    <div class="project-content">
      <section class="hero-section" style={`--hero-bg: url(${optimizedHeroBg.src});`}>
        <div class="hero-content">
          <div class="hero-logo-wrap reveal-scale">
            <img src="/grojet-logo.svg" alt="Grojet logo" class="hero-logo" />
          </div>

          <div class="spacer"></div>

          <div class="bottom-section">
            <p class="logo-label reveal">BRAND & MARKETING DESIGN</p>
            <h1 class="project-name reveal">GROJET</h1>
            <div class="project-details reveal" data-stagger>
              <div class="detail-item">
                <p class="detail-label">YEAR</p>
                <p class="detail-value">2025</p>
              </div>

              <div class="detail-item">
                <p class="detail-label">PROJECT</p>
                <p class="detail-value">Grojet</p>
              </div>

              <div class="detail-item">
                <p class="detail-label">ROLE</p>
                <p class="detail-value">Brand & Marketing Designer</p>
              </div>

              <div class="detail-item">
                <p class="detail-label">CATEGORY</p>
                <p class="detail-value">Brand & Marketing Design</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section class="overview-section reveal">
        <h2 class="section-title">Project Overview</h2>
        <p class="overview-text">
          The goal was to design marketing collaterals that resonate with Mangalore's culture, support local merchants, and create a delightful brand interaction beyond just delivery.
        </p>
        <ul class="checklist">
          {checklist.map((item) => (
            <li>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.6"/>
                <path d="M8 12.5l2.5 2.5L16 9" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              {item}
            </li>
          ))}
        </ul>
      </section>

      <section class="highlights-section reveal">
        <h2 class="section-title">Design Highlights</h2>
        <div class="stats-row">
          {stats.map((s, i) => (
            <>
              {i > 0 && <span class="stat-divider"></span>}
              <div class="stat-item">
                <span class="stat-value">{s.value}</span>
                <span class="stat-label">{s.label}</span>
              </div>
            </>
          ))}
        </div>
      </section>

      <section class="quote-section reveal-scale">
        <svg class="quote-icon" width="32" height="32" viewBox="0 0 24 24" fill="currentColor">
          <path d="M7 7c-2.2 0-4 1.8-4 4v6h6v-6H6.2C6.6 9.5 8 8.5 9.5 8.3L9 6c-.7.2-1.4.5-2 1zm10 0c-2.2 0-4 1.8-4 4v6h6v-6h-2.8c.4-1.5 1.8-2.5 3.3-2.7L19 6c-.7.2-1.4.5-2 1z"/>
        </svg>
        <p>Designing more than just promotions — building connections.</p>
      </section>

      <section class="projects-section reveal">
        <p class="projects-label">PROJECTS</p>
        <h2 class="projects-heading">What I Designed for Grojet</h2>

        <div class="filter-row" id="filter-row" data-stagger>
          {filters.map((f, i) => (
            <button class="filter-pill" data-filter={f.key} aria-pressed={i === 0 ? 'true' : 'false'}>{f.label}</button>
          ))}
        </div>

        {categories.map((cat) => (
          <div class="category-block" data-category={cat.key}>
            <div class="category-header">
              <div class="category-heading-group">
                <span class="category-number">{cat.number}</span>
                <h3 class="category-title">{cat.title}</h3>
                <p class="category-desc">{cat.description}</p>
              </div>
              <span class="view-all">
                VIEW ALL
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M5 12h14M13 6l6 6-6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
            </div>
            <div class={`category-images ${cat.layout} ${cat.compact ? 'compact' : ''}`}>
              {cat.images.map((img) => (
                <div class="category-image">
                  <Image src={img.src} alt={img.alt} width={cat.layout === 'single' ? 1000 : 500} loading="lazy" data-cursor-hover />
                </div>
              ))}
            </div>
          </div>
        ))}
      </section>

      <section class="process-section reveal">
        <h2 class="section-title">Design Process</h2>
        <div class="process-row" data-stagger>
          {designProcess.map((step, i) => (
            <>
              <div class="process-step">
                <div class="process-icon">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d={step.icon} stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </div>
                <span>{step.label}</span>
              </div>
              {i < designProcess.length - 1 && <span class="process-line"></span>}
            </>
          ))}
        </div>
      </section>

      <section class="impact-section reveal">
        <h2 class="section-title">Impact</h2>
        <div class="impact-row" data-stagger>
          {impact.map((item) => (
            <div class="impact-item">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d={item.icon} stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>{item.label}</span>
            </div>
          ))}
        </div>
      </section>

      <a href="/gowaterz" class="next-project-btn reveal" data-cursor-hover>
        <span class="next-project-text">
          <span class="next-project-label">NEXT PROJECT</span>
          <span class="next-project-title">GOWATERZ</span>
        </span>
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M7 17L17 7M17 7H7M17 7V17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>

      <footer class="footer">
        <p>© 2026 Portfolio. All rights reserved.</p>
      </footer>
    </div>
  </div>
</Layout>

<script>
  const filterRow = document.getElementById('filter-row');
  const pills = filterRow?.querySelectorAll<HTMLButtonElement>('.filter-pill');
  const blocks = document.querySelectorAll<HTMLElement>('.category-block');

  pills?.forEach((pill) => {
    pill.addEventListener('click', () => {
      pills.forEach((p) => p.setAttribute('aria-pressed', 'false'));
      pill.setAttribute('aria-pressed', 'true');

      const filter = pill.dataset.filter;
      blocks.forEach((block) => {
        const show = filter === 'all' || block.dataset.category === filter;
        block.style.display = show ? '' : 'none';
      });
    });
  });
</script>

<style>
  .project-page {
    background-color: #0a0a0a;
    min-height: 100vh;
    color: var(--color-white);
    position: relative;
    overflow-x: hidden;
  }

  .back-button {
    position: fixed;
    top: 2rem;
    left: 2.5rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-white);
    text-decoration: none;
    font-family: var(--font-syne);
    font-size: 0.75rem;
    font-weight: 400;
    letter-spacing: 1.5px;
    transition: color var(--dur-base, 0.3s) var(--ease-out, ease);
    z-index: 9998;
    opacity: 0.9;
  }

  .back-button:hover {
    opacity: 1;
    color: var(--color-red);
  }

  .back-button svg {
    width: 14px;
    height: 14px;
  }

  .project-content {
    position: relative;
    z-index: 1;
  }

  .hero-section {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    background-color: #0a0a0a;
    position: relative;
    padding: 0;
    overflow: hidden;
  }

  .hero-section::before {
    content: '';
    position: absolute;
    inset: -5%;
    background-image: var(--hero-bg);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    filter: blur(26px) brightness(0.32) saturate(0.75);
    z-index: 0;
  }

  .hero-section::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 45%;
    background: linear-gradient(to top, rgba(10, 10, 10, 1) 0%, rgba(10, 10, 10, 0.85) 30%, transparent 100%);
    pointer-events: none;
    z-index: 1;
  }

  .hero-content {
    position: relative;
    width: 100%;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    z-index: 2;
  }

  .hero-logo-wrap {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -55%);
    z-index: 2;
  }

  .hero-logo {
    display: block;
    width: clamp(200px, 26vw, 380px);
    height: auto;
    border-radius: 28px;
    filter: drop-shadow(0 30px 70px rgba(0, 0, 0, 0.55));
  }

  .spacer {
    flex: 1;
  }

  .bottom-section {
    position: relative;
    z-index: 2;
    padding: 3rem;
  }

  .logo-label {
    font-family: var(--font-syne);
    font-size: 0.75rem;
    letter-spacing: 0.2em;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 1rem;
  }

  .project-name {
    font-family: var(--font-syne);
    font-size: clamp(2.8rem, 8vw, 6rem);
    font-weight: 800;
    color: var(--color-red);
    margin: 0 0 2rem 0;
    letter-spacing: -0.02em;
  }

  .project-details {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    max-width: 1200px;
  }

  .detail-item {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .detail-label {
    font-family: var(--font-syne);
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    color: rgba(255, 255, 255, 0.6);
    margin: 0;
  }

  .detail-value {
    font-family: var(--font-syne);
    font-size: 1rem;
    color: var(--color-white);
    margin: 0;
  }

  .overview-section,
  .highlights-section {
    max-width: 900px;
    margin: 0 auto;
    padding: 3.5rem 3rem;
  }

  .section-title {
    font-family: var(--font-syne);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--color-red);
    margin: 0 0 1.5rem 0;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid var(--color-red);
    display: inline-block;
  }

  .overview-text {
    font-family: var(--font-syne);
    font-size: 1.05rem;
    line-height: 1.75;
    color: rgba(255, 255, 255, 0.8);
    margin: 0 0 2rem 0;
    max-width: 680px;
  }

  .checklist {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .checklist li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-syne);
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.85);
  }

  .checklist li svg {
    flex-shrink: 0;
    color: var(--color-red);
  }

  .stats-row {
    display: flex;
    align-items: center;
    gap: 2.5rem;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .stat-value {
    font-family: var(--font-syne);
    font-size: clamp(2rem, 4vw, 2.6rem);
    font-weight: 800;
    color: var(--color-red);
  }

  .stat-label {
    font-family: var(--font-syne);
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.65);
  }

  .stat-divider {
    width: 1px;
    height: 48px;
    background: #2a2a2a;
  }

  .quote-section {
    max-width: 700px;
    margin: 0 auto 3rem;
    padding: 2.5rem;
    border: 1px solid #2a2a2a;
    border-radius: 16px;
    background: rgba(255, 1, 5, 0.04);
  }

  .quote-icon {
    color: var(--color-red);
    margin-bottom: 1rem;
  }

  .quote-section p {
    font-family: var(--font-syne);
    font-size: 1.3rem;
    font-weight: 500;
    line-height: 1.5;
    color: var(--color-white);
    margin: 0;
  }

  .projects-section {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 3rem 4rem;
  }

  .projects-label {
    font-family: var(--font-syne);
    font-size: 0.7rem;
    letter-spacing: 0.2em;
    color: rgba(255, 255, 255, 0.5);
    margin: 0 0 0.75rem 0;
  }

  .projects-heading {
    font-family: var(--font-syne);
    font-size: clamp(1.8rem, 4vw, 2.6rem);
    font-weight: 700;
    color: var(--color-white);
    margin: 0 0 1rem 0;
    padding-bottom: 1rem;
    border-bottom: 3px solid var(--color-red);
    display: inline-block;
  }

  .filter-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    margin: 2rem 0 3.5rem;
  }

  .filter-pill {
    font-family: var(--font-syne);
    font-size: 0.8rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.75);
    background-color: #1a1a1a;
    border: 1px solid #2a2a2a;
    padding: 0.55rem 1.25rem;
    border-radius: 50px;
    cursor: pointer;
    transition: background-color var(--dur-base, 0.3s) var(--ease-out, ease), color var(--dur-base, 0.3s) var(--ease-out, ease);
  }

  .filter-pill[aria-pressed="true"] {
    background-color: var(--color-red);
    color: var(--color-white);
    border-color: var(--color-red);
  }

  .category-block {
    padding: 2.5rem 0;
    border-bottom: 1px solid #2a2a2a;
  }

  .category-block:last-child {
    border-bottom: none;
  }

  .category-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 1.5rem;
    margin-bottom: 1.75rem;
  }

  .category-number {
    font-family: var(--font-syne);
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--color-red);
    display: block;
    margin-bottom: 0.4rem;
  }

  .category-title {
    font-family: var(--font-syne);
    font-size: clamp(1.3rem, 2.5vw, 1.7rem);
    font-weight: 800;
    color: var(--color-red);
    letter-spacing: 0.02em;
    margin: 0 0 0.5rem 0;
    text-transform: uppercase;
  }

  .category-desc {
    font-family: var(--font-syne);
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
    margin: 0;
    max-width: 420px;
  }

  .view-all {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-syne);
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--color-red);
    letter-spacing: 0.05em;
    white-space: nowrap;
    flex-shrink: 0;
    margin-top: 0.3rem;
  }

  .category-images {
    display: grid;
    gap: 1rem;
  }

  .category-images.row3 {
    grid-template-columns: repeat(3, 1fr);
  }

  .category-images.single {
    grid-template-columns: 1fr;
  }

  .category-images.compact {
    max-width: 420px;
  }

  .category-image {
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid #2a2a2a;
  }

  .category-images.row3 .category-image img {
    aspect-ratio: 9 / 16;
  }

  .category-images.compact .category-image img {
    aspect-ratio: 1 / 1;
  }

  .category-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform var(--dur-base, 0.3s) var(--ease-out, ease);
  }

  .category-image:hover img {
    transform: scale(1.04);
  }

  .process-section,
  .impact-section {
    max-width: 1200px;
    margin: 0 auto;
    padding: 3rem 3rem;
  }

  .process-row {
    display: flex;
    align-items: center;
    gap: 0;
    margin-top: 2.5rem;
  }

  .process-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.85rem;
    text-align: center;
    width: 140px;
    flex-shrink: 0;
  }

  .process-icon {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    border: 1px solid #2a2a2a;
    background: #161616;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-red);
  }

  .process-step span {
    font-family: var(--font-syne);
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.75);
  }

  .process-line {
    flex: 1;
    height: 1px;
    background: #2a2a2a;
    margin: 0 -0.5rem;
    transform: translateY(-22px);
  }

  .impact-row {
    display: flex;
    flex-wrap: wrap;
    gap: 2.5rem;
    margin-top: 2.5rem;
  }

  .impact-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: var(--color-red);
  }

  .impact-item span {
    font-family: var(--font-syne);
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.8);
  }

  .next-project-btn {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: calc(100% - 6rem);
    max-width: 1200px;
    margin: 2rem auto 0;
    background-color: var(--color-red);
    border-radius: 20px;
    padding: 2rem 2.5rem;
    text-decoration: none;
    transition: transform var(--dur-base, 0.3s) var(--ease-out, ease);
  }

  .next-project-btn:hover {
    transform: translateY(-2px);
  }

  .next-project-text {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .next-project-label {
    font-family: var(--font-syne);
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 2px;
    color: rgba(255, 255, 255, 0.7);
  }

  .next-project-title {
    font-family: var(--font-syne);
    font-size: clamp(1.8rem, 5vw, 3rem);
    font-weight: 800;
    color: var(--color-white);
    letter-spacing: -0.02em;
  }

  .next-project-btn svg {
    color: var(--color-white);
    flex-shrink: 0;
  }

  .footer {
    padding: 3rem 3rem 2rem;
    text-align: center;
  }

  .footer p {
    font-family: var(--font-syne);
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.3);
    margin: 0;
  }

  @media (max-width: 768px) {
    .back-button {
      top: 1.25rem;
      left: 1.25rem;
      font-size: 0.65rem;
      gap: 0.4rem;
    }

    .back-button svg {
      width: 12px;
      height: 12px;
    }

    .hero-content {
      padding: 1.5rem 1.25rem 2rem 1.25rem;
      min-height: 100vh;
    }

    .hero-logo {
      width: clamp(140px, 42vw, 220px);
    }

    .bottom-section {
      padding: 1.5rem 1.25rem 2rem 1.25rem;
    }

    .project-details {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.25rem 0.75rem;
    }

    .project-name {
      font-size: clamp(1.7rem, 9vw, 3.2rem);
      line-height: 0.9;
    }

    .logo-label {
      font-size: 0.5rem;
      letter-spacing: 2.5px;
      margin-bottom: 0.5rem;
    }

    .overview-section,
    .highlights-section {
      padding: 2.5rem 1.25rem;
    }

    .stats-row {
      gap: 1.25rem;
    }

    .stat-divider {
      height: 36px;
    }

    .quote-section {
      margin: 0 1.25rem 2.5rem;
      padding: 1.75rem;
    }

    .quote-section p {
      font-size: 1.1rem;
    }

    .projects-section {
      padding: 1rem 1.25rem 3rem;
    }

    .category-header {
      flex-direction: column;
      gap: 0.5rem;
    }

    .category-images.row3 {
      grid-template-columns: 1fr;
    }

    .process-section,
    .impact-section {
      padding: 2.5rem 1.25rem;
    }

    .process-row {
      flex-wrap: wrap;
      justify-content: center;
      gap: 1.5rem 0.5rem;
    }

    .process-line {
      display: none;
    }

    .impact-row {
      flex-direction: column;
      gap: 1.25rem;
    }

    .next-project-btn {
      width: calc(100% - 2.5rem);
      margin: 1.5rem 1.25rem 0;
      padding: 1.5rem 1.5rem;
    }

    .footer {
      padding: 2.5rem 1.25rem;
    }
  }
</style>
